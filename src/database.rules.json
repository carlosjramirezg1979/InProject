{
  "rules": {
    "databases": {
      "database": {
        "documents": {
          "{document=**}": {
            "allow read, write": "request.auth != null"
          }
        }
      }
    }
  }
}
