
{
  "rules": {
    "projectManagers": {
      "$userId": {
        ".read": "request.auth != null && request.auth.uid == $userId",
        ".write": "request.auth != null && request.auth.uid == $userId"
      }
    },
    "companies": {
      "$companyId": {
        ".read": "request.auth != null && resource.data.ownerId == request.auth.uid",
        ".create": "request.auth != null",
        ".update": false,
        ".delete": false
      }
    },
    "projects": {
      "$projectId": {
        ".read": "request.auth != null && resource.data.projectManagerId == request.auth.uid",
        ".update": "request.auth != null && resource.data.projectManagerId == request.auth.uid",
        ".create": "request.auth != null",
        ".delete": false
      }
    }
  }
}
